extends layout

block variables
  - var title = "Create Account"

block content
    h2.text-center Create Account
    body
        form(method="post" action="/users/create")
            input(type="text", name="user",label="username")
            br
            input(type="password", name="pass",label="password")
            br
            input(type="submit")

    footer